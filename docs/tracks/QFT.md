# QFT Demo

Folder:

```text
src/qft/
```

This folder is reserved for unscored QFT, sampling, and recovery support. It
must not affect the scored oracle `ops.bin` contract until a separate QFT or
full-Shor track is explicitly defined.
